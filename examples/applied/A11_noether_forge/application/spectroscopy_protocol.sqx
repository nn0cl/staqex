// Secondary readout: turns the stressed-vs-healthy signal gap into a
// simple confidence score (0..1, saturating) for the detection dossier.

package examples.applied.noether_forge.application

namespace Forge.Spectroscopy {
    pub fn confidence_score(gap: Float) -> Float {
        return gap / (gap + 1.0)
    }
}
