// Real, dimensioned sensor-array coupling parameters (ADR 0195).
//
// Physically plausible order-of-magnitude for NV-center-like sensors, not
// traced to one specific cited measurement (same honesty category as
// A06/A10's SSH treatment). See the README "Units and interpretation".

package examples.applied.noether_forge.domain

namespace Forge.Couplings {
    pub struct SensorCouplings {
        val defect: Energy
        val dip: Energy
    }
}
