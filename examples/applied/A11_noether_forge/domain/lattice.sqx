// Sensor array geometry.

package examples.applied.noether_forge.domain

namespace Forge.Lattice {
    pub enum Boundary {
        Open
    }

    pub struct SensorArray {
        val sites: Int
        val boundary: Boundary
    }

    pub fn middle_index(array: SensorArray) -> Int {
        return array.sites / 2
    }
}
