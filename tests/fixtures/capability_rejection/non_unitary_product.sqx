package fixtures.capability_rejection

pub fn main() -> Unit {
    // Scalar-operator product with scale 2 is mathematical meaning, not a
    // unitary gate. The target lane must reject it without rewriting it.
    Operator Bad = 2.0 * X
    State psi = |0>
    State result = Evolve() { Bad * psi }.run()
    Measure result
}
