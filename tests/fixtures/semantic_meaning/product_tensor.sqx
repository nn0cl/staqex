package fixtures.semantic_meaning

pub fn main() -> Unit {
    Operator grouped = (X * Y) * Z
    Operator non_unitary = 2.0 * X
    State left = |0>
    State right = |1>
    State pair = left *|* right
    State result = Evolve() { non_unitary * pair }.run()
    Measure result
}
