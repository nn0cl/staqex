# LISS-0449–0451 Phase 1 Red Trace

| Field | Value |
|---|---|
| Approval | User approval received 2026-08-20 for Phase 1 Red |
| Scope | WP-0112, WP-0113, WP-0114 acceptance tests and fixtures |
| Excluded | Production code, Phase 2 Green, provider SDK, live QPU, S02 numerical migration |
| Review lenses | physicist-first meaning, ideal/finite boundary, canonical provenance, capability honesty, atomic rejection |

## Design check

- Tests are placed at the repository `tests/` root per the testing strategy.
- Fixtures are source-visible and keep ideal expressions separate from finite
  realization policy.
- The tests assert observable semantic IR and target artifact envelopes; they
  do not call a provider or require network access.
- The intended failures identify missing contract fields or incorrect target
  boundary behavior, not syntax redesign.

## Added Red artifacts

- `tests/test_liss_0449_ideal_realization_boundary_red.py`
- `tests/test_liss_0450_semantic_ir_meaning_red.py`
- `tests/test_liss_0451_qpu_capability_rejection_red.py`
- fixtures under `tests/fixtures/ideal_realization/`,
  `tests/fixtures/semantic_meaning/`, and
  `tests/fixtures/capability_rejection/`

## Verification

`.venv/bin/python -m pytest` on the three new suites: **9 failed, 1 passed**.
The failures are intentional Red evidence for the approved contracts. The
two passing checks cover existing realization provenance fields and the
current rejection envelope; no production behavior was changed.

The S02 read-only audit is recorded separately in
`docs/collaboration/traces/2026-08-20-liss-0452-s02-corpus-audit.md`.

## Gate

Phase 1 Red is complete for the bounded test packet. Phase 2 Green requires a
separate typed approval after review of these tests. No implementation was
started.
