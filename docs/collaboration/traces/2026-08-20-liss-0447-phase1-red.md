# LISS-0447 Phase 1 Red Trace

- Approval: user typed `承認` for LISS-0447 Phase 1 Red.
- Branch: `codex/liss-0438-residual-reconciliation`.
- Allowed paths: `tests/test_liss_0447_residual_semantic_consumers_red.py`,
  `tests/fixtures/residual_semantic_consumers/`, and LISS-0447 review/trace/
  Issue/Spec/WP records.
- Excluded: all production implementation, deletion, provider/network,
  live-QPU, S02, solver, syntax, and LISS-0446 changes.
- Baseline: worktree already contained unrelated production/document changes
  from prior LISS-0444/LISS-0445/LISS-0446 work. LISS-0447 did not modify
  those paths.

## Red execution

Command:

```text
.venv/bin/pytest -q tests/test_liss_0447_residual_semantic_consumers_red.py
```

Result before the mismatch-evidence correction: 6 failed, 0 passed, no
collection errors. After adding the separate mismatch case: 7 failed, 0
passed, no collection errors.

The failures are intentional: canonical Realize fields/record, H1 canonical
authority, ordinary QASM fallback retirement, unsupported rejection code, and
the mismatched plan/projection contract are not implemented.

## Gate

Phase 1 Red is complete. Phase 2 Green requires a separate typed approval and
must be performed one subcontract at a time, starting with AlgorithmPlan.

Independent Phase 1 Red review 02: **READY**. The reviewer noted that the
mismatch case currently proves explicit `ValueError` rejection; exact
diagnostic code/message promotion remains a Phase 2 Green contract.
