# LISS-0438 Phase 1 Red trace

## [DESIGN CHECK]

- Scope and expected behavior: create only the failing acceptance tests for
  the S02 residual reconciliation. Freeze the exact local lane, require a
  source-visible formal `Limit` and finite `Realize` lane, and require the
  benchmark report to distinguish exact output, finite-target provenance, and
  capability rejection.
- Specifications and files inspected: LISS-0438, WP-0104, ADR 0210, the
  accepted explicit-evolution surface, `main_selection.sqx`, the frozen S02
  baseline, the existing benchmark report, and the testing strategy.
- Component boundaries, ports/adapters, and VO/DTO candidates when applicable:
  no production component or port is changed in Phase 1; the tests describe
  the future comparison/provenance record boundary.
- Applicable constraints: Phase 1 Red only; no production implementation, no
  S02 numerical retuning, no live QPU, provider SDK, credentials, network, or
  broad corpus migration.
- Decisions, assumptions, and unresolved ambiguities: the accepted S02
  baseline remains the seed-0/source-hash/20-shot record already committed;
  finite-target execution remains a later provider-neutral target-plan step.
- Included and omitted AI context: included the cited Issue, Spec, WP, ADR,
  current S02 source and benchmark artifacts; omitted provider details and
  unrelated examples.
- Task routing (model/assistant/tool): deterministic source, hash, and test
  checks; independent review is required after Red creation.
- Input/output evidence contract when AI output is involved: no AI-generated
  runtime or physics result; all acceptance claims are file-backed and
  deterministic.
- Independent review lenses selected and why: contract completeness, source
  fidelity, dimensional/physics boundary, realization fail-closed behavior,
  reproducibility, and phase/approval discipline.
- Verification plan: run the new suite as a plain script and with the
  repository's available test runner; record expected Red failures, then
  obtain a fresh read-only independent review before requesting Phase 2.

## Approval and scope

- User approval: `承認` on 2026-08-18, interpreted as typed approval for
  LISS-0438 Phase 1 Red only.
- Allowed changes: this Red test file and process trace/status updates.
- Forbidden changes: compiler/runtime/source implementation, S02 model or
  numerical baseline, live QPU/provider integration, and Phase 2 Green.

## Red evidence

- `python3 -m py_compile tests/test_liss_0438_residual_reconciliation_red.py`:
  PASS.
- `python3 tests/test_liss_0438_residual_reconciliation_red.py`: expected Red;
  `test_exact_local_lane_is_still_source_visible` PASS,
  `test_pre_migration_baseline_is_frozen_for_phase_1` PASS,
  `test_target_lane_spells_formal_limit_and_finite_realize` FAIL because the
  current S02 source has no `U_formal`/`U_qpu` target lane, and
  `test_benchmark_report_separates_exact_and_finite_lanes` FAIL because the
  current report has no explicit exact/finite/provenance/rejection fields.
- `python3 -m pytest ...`: unavailable locally because pytest is not installed;
  the repository's CI runner remains the aggregate pytest environment.
- `git diff --check`: PASS.

The failures are intentional and are the Phase 1 acceptance boundary. No
production implementation or S02 numerical artifact was changed.

## Independent review disposition

The fresh read-only review found four findings. The baseline hash assertion
was accepted and corrected: Phase 1 freezes the pre-migration artifact, while
Green may produce a distinct post-change source hash. The R1 completeness and
report evidence assertions were accepted and added. The request to duplicate
P8/P10 runtime behavior in this residual test was rejected as already covered
by the accepted LISS-0437 suites (`tests/test_liss_0437_limit_realization_red.py`
and `tests/test_liss_0437_explicit_evolution_surface_red.py`); LISS-0438
asserts the S02 comparison boundary and references those authoritative policy
tests rather than creating a second policy surface.

Disposition authority: primary agent under the accepted LISS-0438 Spec/WP
boundaries; no architecture, technology, Issue, or phase deviation.

## Review terminal state

- Independent review record: [2026-08-18 Phase 1 Red review](../reviews/2026-08-18-liss-0438-phase1-red-review.md)
- Terminal state: `COMPLETE`
- Verdict: `READY` for Phase 1 Red closure; Phase 2 Green is not approved.
- Remaining requirement: obtain separate typed implementation/Phase 2 approval;
  Green must test outcome-dependent provenance retention semantics.
