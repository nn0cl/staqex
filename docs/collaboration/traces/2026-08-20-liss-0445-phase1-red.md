# LISS-0445 Phase 1 Red trace

- User approval: Phase 1 Red approved after the LISS-0445 design review.
- Allowed changes: fixed Red test, fixed semantic migration fixture, and
  Issue/Spec/WP/review/trace documents only.
- Added `tests/test_liss_0445_consumer_migration_red.py` and
  `tests/fixtures/semantic_consumer_migration/ordinary_gate.sqx`.
- Verification: `.venv/bin/pytest -q tests/test_liss_0445_consumer_migration_red.py`
  → **5 failed, 7 passed**, no collection errors.
- Red failures identify the intended missing migration: duplicate executable
  Algorithm Plan authorities, diagnostic-side binder reconstruction,
  repeated binder projection during compilation, H1 early-return bypass, and
  ordinary QASM AST fallback.
- No production code or migration was performed. Phase 2 Green requires a new
  typed approval after independent Red review.
- Baseline boundary: Phase 1 began on the existing non-main branch with
  pre-existing production changes from LISS-0444/WP-0107 visible in the
  worktree. The Phase 1 diff is therefore not used to attribute those earlier
  production changes; this phase added only the fixed Red test, fixture, and
  its Issue/Spec/WP/review/trace documentation.
- Reproducible baseline evidence captured at review correction: base commit
  `5bb751a500ea35e4706a56e3551f445c5d469596`, branch
  `codex/liss-0438-residual-reconciliation`, and pre-existing production paths
  `compiler/staqex/backend/qasm/emitter.py`, `compiler/staqex/pipeline.py`,
  `compiler/staqex/qpu_ir.py`, `compiler/staqex/quantum_semantic_ir.py`, and
  untracked `compiler/staqex/scientific_semantic_ir.py`.
- Final independent review: **READY / COMPLETE**. The review loop accepted
  the behavior-level Algorithm Plan projection test and the executable caller
  DTO/string DTO negative tests. No Phase 1 blocker remains. H1/QASM
  provenance completeness and full Realize policy-field retention remain
  Phase 2 Green candidates and require a separate typed approval.
- User approval received for Phase 2 Green. This records phase approval only;
  implementation approval remains a separate gate under the repository
  operating contract.
- User implementation approval received for the bounded binder canonical
  projection slice. The implementation changed only the approved semantic IR,
  QPU diagnostic, pipeline, QASM diagnostic call-site, and focused test call.
- Phase 2 verification: focused suite **9 passed, 3 failed**; related
  regression **27 passed**; full regression **1659 passed, 3 failed**. All
  three failures are the explicitly excluded Algorithm Plan, H1, and ordinary
  QASM fallback Red contracts.
- Existing production changes visible in the worktree belong to the prior
  LISS-0444 bounded projection work; this Phase 1 added only the fixed Red
  test, fixture, and its Issue/Spec/WP/review/trace documentation.
