# LISS-0444 Phase 1 Red trace

## Gate and scope

- Issue / Spec / WP / ADR: LISS-0444 / Scientific Semantic Core / WP-0107 /
  ADR 0211
- Branch: `codex/liss-0438-residual-reconciliation`
- Approval: user approved WP-0107 Phase 1 Red on 2026-08-19.
- Allowed changes: `tests/test_scientific_semantic_core_red.py` and
  `tests/fixtures/semantic_core/`, plus this trace and status metadata.
- Excluded: Scientific Semantic IR implementation, consumer rewiring or
  deletion, provider SDK, live QPU submission, S02 numerical migration,
  solver implementation, and Green/implementation work.

## Design and evidence contract

- The tests target `ssc-semantic-v1`, `SemanticInspectionResult`,
  `SemanticRejection`, source-derived structural nodes, projection
  conservation, explicit `Realize`, terminal/dynamic measurement boundaries,
  and no independent legacy authority.
- The fixtures are SSC-001 through SSC-009 from the approved Spec.
- Red means the current implementation fails the new contract; it does not
  authorize weakening assertions or modifying production behavior.

## Verification

- Command: `.venv/bin/pytest tests/test_scientific_semantic_core_red.py -q`
- Result: `2 passed, 15 failed`.
- Expected Red evidence: current `CompileResult` has no
  `scientific_semantic_ir`, inspection/rejection/snapshot contracts, or
  canonical source-node fields on downstream projections. One corpus
  completeness test passes; contract tests fail at the missing target API.
  The caller-injection test separately fails because the current
  `lower_hir_to_physics_ir(..., equations=...)` still accepts the injected
  `EquationNode`, which is the intended Red evidence for that legacy path.
- The proof-map consistency test passes, confirming all 13 proof IDs have
  explicit test targets.
- Existing production files changed: none.
- Next gate: independent read-only Red-test review, then separate user approval
  is required before Phase 2 Green or implementation.
