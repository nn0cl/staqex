# LISS-0510 lexical scope and State shadowing Red trace

## Request and phase

- Date: 2026-09-06. User confirmed ADR 0216 and requested continuation.
- Feature Path, size M, Phase 1 Red only.
- Authority: ADR 0216 and the quantum mental-model follow-up specification.
- Branch: `codex/liss-0510-lexical-scope`.

## Context ledger

- Included: ADR 0216, LISS-0510, WP-0092, parser BlockExpr, typecheck block
  handling, scientific lexicon contract, existing linearity diagnostics and
  process lessons.
- Omitted: provider/QPU, Rust, public observation types, unrelated scope systems.
- Decision applied: braces and block expressions are the source-visible scope
  boundary; one binding environment must drive type, linearity and metadata.
- No new dependency, port or provider decision.
- Routing: same_context review, host implementation; model identifiers and
  token usage unavailable.

## Red evidence

Added `tests/test_liss_0510_lexical_scope_red.py` with four scenarios:
inner State shadow and outer restoration, cross-type inner shadow, same-scope
duplicate rejection, and per-binding State consumption. The current compiler
does not expose scoped binding identity/declaration spans or the required
duplicate diagnostic, so the suite is intended to be Red. One scenario also
captures the current BlockExpr limitation: it permits `let`-based expression
blocks rather than general statement blocks.

No production implementation was changed. Next safe action is Adjudicator
review of the four Red acceptance tests, followed by Phase 2 only after review.
