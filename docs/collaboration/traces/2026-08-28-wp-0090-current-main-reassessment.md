# AI work trace: WP-0090 current-main reassessment

| Field | Value |
|---|---|
| Date | 2026-08-28 |
| Work plan | [WP-0090](../../work-plans/WP-0090-documentation-canonicalization.md) |
| Scope | Re-inventory current documentation after later merges |
| Phase | Reassessment / documentation-only |
| Branch | `docs/wp-0090-reassessment` |
| Implementation | No runtime or compiler changes |

## Findings

- The baseline tag `docs/pre-canonicalization-2026-08-03` remains available at
  commit `8663ba72295964069ac275b93c350e762a0844d8`.
- The current `main` tree has 1,022 `docs/` files, 983 Markdown files, 37
  ADRs, 239 Issues, 81 Work Plans, and 234 Traces.
- The compression map's historical snapshot reported 562 files after the
  original batch; that value is stale because later merges added current
  records. The map now records the current-tree counts separately.
- `scripts/documentation_compression.py` reports 138 candidates. This is not a
  safe deletion set: completed current-generation Issues and Work Plans are
  still retained as acceptance or review evidence.
- The old `docs/documentation-canonicalization` branch is based on an older
  history and includes unrelated compiler changes. Its commits were not
  merged or reused.

## Disposition

No document was deleted in this reassessment. The correct next unit is a
small [classifier-correction Issue](../../issues/LISS-0473-documentation-compression-classifier.md)
that distinguishes historical records from current-generation acceptance
evidence, followed by a reviewed candidate list. Until that exists, the central map and all current source records remain
the safer state.

## Verification evidence

- Baseline recovery remains `git show docs/pre-canonicalization-2026-08-03:<path>`.
- `git status` was clean before this documentation-only update.
- Candidate inventory was generated by `python3 scripts/documentation_compression.py`.
- A relative-link scan found seven pre-existing missing local targets in
  `ADR-0204`, `ADR-0210`, and two LISS-0445 review/trace records. They are
  outside this reassessment's safe scope and remain a separate documentation
  integrity follow-up; no unrelated link repair was applied here.
- LISS-0473 Phase 1 Red was then added and confirmed by the intentionally
  missing `classify_records` API; implementation remains gated.
