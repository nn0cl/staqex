# AI Work Trace — LISS-0577 Phase 0 design

## Request

- Date: 2026-09-24
- User request: continue evaluator decomposition after LISS-0576 merge.
- Current phase: complete; final closeout commit verification pending.
- Canonical issue / plan: LISS-0577 / WP-0170 (proposed).
- AI planning record: AIP-0577-001.

## Context ledger

- Included: merged `origin/main` at `9b2e2e0f`, decomposition specification,
  Evaluator and classical/execution/context successors, direct static consumer
  search, runtime routing, process lessons, quality and verification policy.
- Omitted: unrelated parser/typechecker, provider/QPU code, and private data.
- Assumption: Phase 2 work is limited to the approved body extraction and
  compatibility wiring; no semantic change is intended.
- Open decisions: none on LISS-0577 scope or review. Closeout commit
  verification is run separately.

## Routing and lessons

- Route: host agent plus deterministic AST, ripgrep, and Git inspection.
- Review: `same_context` unless live routing changes.
- Applied lessons: `decomposition-callback-boundary` and
  `private-consumer-inventory` require explicit inventory of cross-module and
  underscore-prefixed callers; `evaluator-state-ownership` keeps all mutable
  runtime state in `Evaluator`. The open LISS-0576 `status-drift` lesson
  requires approval/status transitions to be synchronized across the canonical
  spec, Issue, WP, and trace; these artifacts are being updated together.
  `red-fixture-interface` was applied: the first direct fixture probe caught
  semicolon syntax rejected by the parser; the test fixture was corrected and
  its characterization re-run.
  `refactor-branch-preservation` is applied in Phase 3: helper extraction was
  kept scoped, the branch structure was reviewed, and the exact specification
  suite was rerun.

## Evidence and decisions

- `origin/main` at `9b2e2e0f` has `evaluator.py` at 1,867 lines and 82 direct
  `Evaluator` methods.
- Four classical call-evaluation bodies form a 259-line contiguous family.
- Static callers include value `Call` dispatch in `evaluation/classical.py`,
  classical-result binding in `evaluation/execution.py`, and Evaluator-local
  dispatch. No direct test source mention of these private method names was
  found; behavior tests are identified by historical issue references and need
  a complete Phase 0 test inventory.
- Mutable/frame dependencies include function/class maps, objects/scalars and
  units, receiver and frame state, local unit restoration, nested value
  evaluation, and assignment execution.
- Initial proposal was a focused `evaluation/classical_calls.py`; the boundary
  and state authority were subsequently accepted by the Adjudicator below.
- At initial Phase 0 design, implementation authorization was none; Phase 2
  authorization was granted separately below.

## Adjudicator decision

- `LISS-0577 Architecture approval 承認` received 2026-09-24. Accepted the
  four-method classical-call family, Evaluator state/DTO ownership, focused
  successor module, and compatibility entrypoints. This does not authorize
  Phase 1 tests or implementation.
- `LISS-0577 Phase 0 acceptance 承認` received 2026-09-24. Accepted the
  consumer/behavior inventory, narrow module-local context proposal, and exact
  phase paths. Separate Phase 1 approval was then obtained.
- `Phase 1 Red承認` received 2026-09-24. Tests and test-only fixtures are
  authorized within the accepted Phase 1 paths; implementation remains
  unauthorized.
- `Phase 1 Redテスト承認` received 2026-09-24. Test contracts are accepted;
  this does not authorize production implementation.
- `LISS-0577 Phase 2 Green / Implementation 承認` received 2026-09-24.
  Approved only the named Phase 2 production paths; Phase 3 remains pending.
- `Phase 2 Green承認` received 2026-09-24. Phase 2 is accepted; Phase 3
  Refactor remains unauthorized.
- `LISS-0577 Phase 3 Refactor 承認` received 2026-09-24. Authorized the
  behavior-preserving refactor in the accepted classical-call family.
- `別ゲートの最終レビュー承認` received 2026-09-24. Accepted Phase 3 and
  the bounded 324-line structure disposition; no further module split was
  requested. A commit-specific full suite is recorded below.
- The implementation/review commit `5ca2130b3889482d70e5f2f9df7bb8603b1ea2d2`
  passed all-blocking tests: **2,255 passed, 0 failed in 326.46s**.
- Phase 0 consumer and characterization inventory was then completed. The
  proposed context contract is module-local, avoiding expansion of the shared
  `EvaluatorContext`; this inventory was completed before the acceptance above.

## Verification

- The project's existing `.venv` at the main checkout was reused; it already
  contains Python 3.14.6 and pytest 9.1.1, so no environment was created or
  modified.
- Phase 1 focused pytest on the approved Red tree: 3 expected structural
  failures, 3 passed characterizations.
- Phase 2 focused pytest: LISS-0577 **6 passed**; consumer suites **32 passed**;
  adjacent frame/stateful-call suites **16 passed**; all-blocking suite **2,255
  passed, 0 failed** in 331.53 seconds on the final Phase 2 source state.
  All-blocking command: `/Users/nn0cl/Documents/git/qpex/.venv/bin/python -m
  pytest -p no:cacheprovider tests/ -q`, cwd
  `/Users/nn0cl/.codex/worktrees/liss-0577-classical-call-evaluation/qpex`.
  Evidence is for dirty working tree at HEAD/base SHA
  `9b2e2e0f8e56399ba8cbd662bbc3bf6d569cb7b7`, macOS 27.0 / Darwin 27.0, Python
  3.14.6, pytest 9.1.1; run window approximately 2026-09-24 10:54–11:00 JST.
  No comparable baseline suite was run. CI/final-commit verification remains
  required.
- Phase 3 verification on the dirty working tree at the same base SHA and
  environment: focused + consumer + adjacent command covering 54 tests passed
  in 0.67s; all-blocking command reported 2,255 passed, 0 failed in 325.21s,
  approximately 2026-09-24 11:19–11:24 JST. The Phase 2 all-blocking run in
  the same environment also had 2,255 passed, 0 failed; no failure IDs were
  introduced or remain unresolved. This is provisional until a final commit
  SHA is tested.
- In-memory syntax compile, active-Red lifecycle, document lifecycle,
  coverage-ledger consistency, and `git diff --check` pass.

## AI Execution Record — Phase 2

- Agent/environment: Codex host agent; macOS, existing project `.venv`, Python
  3.14.6; model and reasoning setting are not exposed by the host UI.
- Scope/result: moved four approved classical call bodies, added the narrow
  call-local-units accessors and compatibility wiring, and ran focused,
  consumer, adjacent, and all-blocking verification; no semantic test changes.
- Actual token use: unavailable; the host did not expose per-turn token usage.
- Attempt boundary: one Phase 2 implementation and verification attempt.

## AI Execution Record — Phase 3

- Agent/environment: Codex host agent; same-context reviewer route; existing
  `.venv`, Python 3.14.6; model and reasoning setting are not exposed by the
  host UI.
- Scope/result: consolidated repeated method resolution and return-expression
  selection within `evaluation/classical_calls.py`; preserved accepted
  module ownership, compatibility hooks, runtime state ownership, and tests.
- Review: same-context review was used because `runtime-routing.toml` sets
  `review.isolation = "same_context"`; this is weaker than a separate-context
  review. No model switch was available.
- Actual token use: unavailable; the host did not expose per-turn token usage.
- Attempt boundary: one Phase 3 refactor and review attempt; one formatting
  correction was followed by a fresh full blocking run.

## Changed files

- Spec, Issue, work plan, this trace, Phase 1 test file, active-Red lifecycle
  entry removal, `evaluator.py`, `evaluation/classical_calls.py`, and
  `evaluation/compatibility.py`; Phase 3 Review Summary.

## Next safe action

- LISS-0577/WP-0170 are synchronized to done with a no-deviation process
  review; run blocking tests on the final closeout commit before continuing.
