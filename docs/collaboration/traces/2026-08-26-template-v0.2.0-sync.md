# llm-project-template v0.2.0 sync

## Scope and approval

- Request: import the latest `llm-project-template` release `v0.2.0`.
- Source: `git@github.com:nn0cl/llm-project-template.git`.
- Source tag: `v0.2.0`, commit `384f2fed613e0650c020cae0b714c543f97113e7`.
- Previous adopter ref: `9954c57c955fa883e030182ed9b070484dc82177`.
- Operation: Architecture/Process Path template synchronization on a
  dedicated branch; no application runtime behavior is changed.

## Routing and evidence

- The repository's existing synchronization script was run from a temporary
  copy because the target script is itself a synchronized path and would
  otherwise be replaced while executing.
- Tier 1 template files were synchronized by the script.
- Tier 2 context files were reviewed against the Staqex adopter facts and then
  updated to the v0.2.0 template form; facts were moved to
  `docs/collaboration/project-conventions.md`.
- v0.2.0 additions not covered by the old path list were copied from the
  source: `.agents/skills/**`, `scripts/configure-ai-collaboration.sh`, and
  `scripts/check-document-lifecycle.py`.

## Boundary decisions

- Template process rules are imported as template-authoritative process
  content.
- Staqex facts remain target-owned in `project-conventions.md`: State-first
  semantics, terminal measurement, Scientific Semantic IR authority, ports,
  deferred provider/deployment decisions, and physicist-first source meaning.
- The target-specific coverage check was updated to read the LISS-0129 shipped
  fact from `project-conventions.md`, preserving the v0.2.0 rule that template
  context files do not carry adopter facts.
- No provider, datastore, deployment technology, or language meaning was
  selected by this synchronization.

## Verification plan

- `bash -n` for all shell scripts.
- `python3 -m py_compile` for repository Python checks.
- `git diff --check`.
- Template/document lifecycle and existing deterministic specification checks.

## Remaining gate

The synchronization branch must receive review of the template-owned context
changes and `project-conventions.md` before its PR is merged.

## Script smoke test

- Ran the v0.2.0 source checkout's `copy-ai-collaboration-files.sh` against a
  disposable Git repository: project-name, domain-summary, and stack
  substitutions succeeded; only history-directory `.gitkeep` files were
  retained; the sync marker pointed to `384f2fed613e0650c020cae0b714c543f97113e7`.
- Ran `configure-ai-collaboration.sh --non-interactive` against the same
  disposable repository and the adopter checkout. Both produced the expected
  target-owned `docs/collaboration/runtime-routing.toml` with
  `review=same_context`, `implementation=host`, and empty model identifiers.
- Ran `init-llm-context.sh` against the disposable repository; required-file
  validation and prompt generation passed.
