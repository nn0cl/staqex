# LISS-0449–0451 Phase 2 Green Trace

| Field | Value |
|---|---|
| Scope | Ideal/finite boundary, semantic meaning IR, finite-target capability rejection |
| Issue / WorkPlan | LISS-0449–0451 / WP-0112–0114 |
| Branch | `codex/liss-0438-residual-reconciliation` |
| Phase | Phase 2 Green; independent re-review pending |
| Allowed changes | `compiler/staqex/backend/qasm/`, `compiler/staqex/scientific_semantic_ir.py`, related tests and review records |
| Excluded | Phase 3, PR merge/push, live QPU, provider SDK, S02 numerical migration, credentials/network |

## Implemented corrections

1. `Coin` and generic mixture `when` no longer become implicit `H` or
   `H+RZ(0)` target gates; they reject at the finite projection boundary with
   mixture provenance.
2. Static target preflight accounts for QubitRegister sizes, static register
   expansion, and quantum State binds before gate allocation. The finite
   binder path retains its more precise mapping/domain/budget evidence.
3. `ExactExponential` is marked exact in the semantic IR. Its finite-target
   decision inspects only operator bindings referenced by that exponential,
   rather than an unrelated binder anywhere in the unit. The ideal meaning
   fingerprint includes the source-owned structural meaning fields.
4. QASM emitter resource and canonical-projection failures return an empty
   target envelope with explicit rejection provenance.

## Verification

- Focused Green and boundary regression: **35 passed**.
- Related QASM/semantic/public-entry regression: **67 passed**.
- Broad suite without known out-of-scope suites was interrupted in the long
  numerical path after **841 passed**; it did not reach a final suite result.
- Known legacy contract conflicts remain outside this Phase 2 packet:
  plain implicit exact-exponential lowering tests, live QPU submit tests, and
  older QASM expectations that predate the explicit finite-boundary contract.

## Review gate

The first independent Green review returned `NOT READY`; all four findings
were accepted as design-preserving and corrected in this phase. A fresh
independent read-only reviewer was requested after correction. Phase 3 and
merge approval are not granted by this trace.
