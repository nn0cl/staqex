# LISS-0449–0451 Phase 3 Refactor Trace

## [DESIGN CHECK]

- Scope and expected behavior: behavior-preserving Phase 3 refactor for the
  accepted ideal/finite boundary and QPU rejection implementation. Improve
  readability and centralize the repeated empty rejection-envelope invariant.
- Specifications and files inspected: LISS-0449–0451, WP-0112–0114,
  `staqex-qpu-capability-rejection-contract.md`, ADR 0210–0212,
  `compiler/staqex/backend/qasm/emitter.py`, `lower.py`,
  `scientific_semantic_ir.py`, and the Phase 2 review records.
- Component boundaries, ports/adapters, and VO/DTO candidates: no new
  boundary or dependency. `Circuit` remains the target artifact DTO;
  canonical Scientific Semantic IR remains the semantic authority.
- Applicable constraints: no behavior or assertion changes; no implicit
  finiteization; no provider/live-QPU/S02 work; no new architecture or
  technology.
- Decisions, assumptions, and unresolved ambiguities: Phase 3 is limited to
  readability, duplication reduction, and reviewer empathy documentation.
  Any behavior discrepancy stops the refactor.
- Included and omitted AI context: included the scoped implementation,
  accepted specs/ADRs, readiness checklist, review ledger, and focused tests;
  omitted unrelated compiler backends, provider code, S02 numerical code, and
  full repository history.
- Task routing: deterministic source inspection and pytest for verification;
  independent read-only review after refactor.
- Input/output evidence contract when AI output is involved: no AI-generated
  runtime data is consumed; review output must contain paths, priorities,
  readiness, and reusable perspectives without hidden reasoning.
- Independent review lenses selected and why: realization/fail-closed,
  canonical authority, executable projection integrity, source-to-domain
  fidelity, migration/regression safety, and phase/approval discipline.
- Verification plan: focused 35-test boundary suite, related 68-test suite,
  `git diff --check`, and independent Phase 3 review. Assertions and expected
  rejection codes must remain unchanged.

## Phase 3 approval

- Phase: Phase 3 Refactor.
- Approval type: implementation approval.
- Approved by user: 2026-08-21.
- Expires: current bounded execution attempt.
- Post-review: required before Phase 3 closeout.

## Terminal review state

- Independent review loop: `COMPLETE`.
- Latest review: `docs/collaboration/reviews/2026-08-21-liss-0449-0451-phase3-refactor-review-03.md`.
- Result: `READY`; no unresolved review blocker.
- Issue/WP state: `complete` after inclusion in PR #559 and merge into `main`.
- Provider/live-QPU work remains separately gated and is not authorized by
  this trace.

## Refactor result

- Centralized the repeated empty target rejection envelope in
  `QASM3Emitter._empty_rejection_circuit`; the helper preserves zero qubits,
  zero bits, empty gates, allocation state, partial program, and provenance.
- Made the canonical executable-instruction condition explicit while keeping
  Measure-only canonical projections on the existing generic projection
  diagnostic path. Added a regression test for that boundary.
- No provider, language-surface, semantic, or target-capability behavior was
  intentionally added.

## Verification result

- Focused boundary suite: **35 passed**.
- Related QASM/semantic/public-entry suite: **68 passed**.
- Additional Measure-only projection regression: passed as part of the
  related suite.
- `python3 -m py_compile` for touched production modules: passed.
- `git diff --check`: passed.

Reproduction commands and outputs:

```text
./.venv/bin/python -m pytest tests/test_qasm3_codegen.py \
  tests/test_qpu_ir_boundary_red.py \
  tests/test_scientific_semantic_core_red.py \
  tests/test_openqasm_ch0_integrated_red.py \
  tests/test_liss_0446_qasm_public_entry_red.py -q
.................................................................... [100%]
68 passed in 0.34s

./.venv/bin/python -m pytest \
  tests/test_liss_0449_ideal_realization_boundary_red.py \
  tests/test_liss_0450_semantic_ir_meaning_red.py \
  tests/test_liss_0451_qpu_capability_rejection_red.py \
  tests/test_liss_0437_phase3_red.py \
  tests/test_liss_0372_rx_angle_silent_gate_drop_red.py \
  tests/test_liss_0446_qasm_public_entry_red.py -q
................................... [100%]
35 passed in 0.23s

python3 -m py_compile compiler/staqex/backend/qasm/emitter.py \
  compiler/staqex/backend/qasm/lower.py \
  compiler/staqex/scientific_semantic_ir.py
passed

git diff --check
passed
```

## Independent review disposition

The first Phase 3 review returned `NOT READY`:

1. P1 requested explicit regression evidence for the Measure-only canonical
   classification. Accepted as a testability correction; the regression test
   above now freezes the expected diagnostic path.
2. P2 requested executed results and a reviewer empathy summary. Accepted;
   this trace now records both.
3. The final re-review requested reproducible commands and output. Accepted;
   the exact commands and observed output are recorded above.

Reviewer empathy summary:

- A physicist reading the source should see that an ideal/semantic expression
  is not silently turned into a gate; rejection remains an explicit target
  boundary.
- A programmer reviewing QASM rejection should find one envelope constructor,
  deterministic codes, and no residual allocation artifacts.
- A future maintainer should treat Measure-only canonical data as diagnostic
  evidence, not as an executable finite projection.
