# Independent Context Review: LISS-0449–0451 Phase 1 Red Review 03

| Field | Value |
|---|---|
| Trigger | Fresh review after Phase 1 Red Review 02 corrections |
| Context | Fresh read-only reviewer context |
| Scope | Red tests, fixtures, traces, Specs/WPs, ADR 0212 |
| Verdict | **NOT READY** |
| Phase 2 / implementation approval | **not granted** |
| Loop terminal state | **not terminal; correction required** |

## Disposition of prior findings

| Finding | Disposition | Evidence |
|---|---|---|
| Exact exponential fixture validity | accepted/resolved | fixture compiles with Energy/Time/hbar units; test requires `ExactExponential` semantic identity |
| Non-unitary fixture validity | accepted/resolved | `X * Y` source product compiles and reaches `lower_unit_to_circuit` |
| Mixture target connection | accepted/resolved | mixture fixture contains explicit evolution and target-lowering assertion |
| Ideal retention after projection | accepted/resolved | test invokes lowering before checking the canonical ideal result |
| Resource preflight path | accepted/resolved as Red | valid register fixture reaches lowering and fails because allocation still occurs before budget rejection |
| Limit identity | accepted/resolved as Red contract | test directly requires the specified `Limit` semantic identity, without generic `Call` fallback |

## Review conclusion

The fixtures compile and reach the relevant paths, but two tests stopped at a
semantic assertion before exercising their target rejection assertion. WP-0112
also omitted the fixed exact-exponential fixture. These are accepted Red-test
corrections, not Green implementation work. S02 remains a separate read-only
audit and no source change is implied.

Phase 1 Red is not ready for the next approval gate. Phase 2 Green remains
unauthorized.

The later independent review identified that `X * Y` is unitary up to phase;
that fixture choice is rejected and is corrected to `2.0 * X` in the next
iteration.

## Reusable reviewer perspectives

- Verify fixture validity and semantic/target-path reachability separately.
- Assert contract-specific IR identity, rejection code, reason, and provenance;
  do not accept a generic error as evidence of the intended boundary.
- Keep ideal meaning retention, target rejection, and linear resource checks as
  separate observable assertions.
- Reconcile Red counts with the exact command output in the same trace.
