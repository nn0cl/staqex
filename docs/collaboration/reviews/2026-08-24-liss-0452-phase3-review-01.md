# LISS-0452 Phase 3 Independent Review 01

## Review scope and boundary

- Trigger: Phase 3 Refactor closeout review after user approval.
- Independent context: fresh read-only reviewer; no edits, approval, merge, or
  push.
- Branch under review: `docs/liss-0452-corpus-audit` at `69317b00`.
- Inspected: LISS-0452, WP-0115, Phase 3 trace, Phase 0 audit/reviews, S02
  README/source/host report, and the focused Red/regression tests.

## Findings

1. **P1 — finite-target rejection envelope requires an unresolved contract
   decision.** Direct lowering returns `QASM_TROTTER_UNSUPPORTED_H`, empty
   gates, `n_qubits=1`, `allocation_started=False`, no allocated qubits,
   `partial_program=None`, and no provenance. The generic LISS-0451 rejection
   contract expects `n_qubits=0` and provenance-bearing rejection. It is not
   safe to decide whether the one-qubit shell is intentional without
   Adjudicator direction.
2. **P1 — Phase 3 review evidence was not present at the reviewed commit.**
   This record and the Phase 3 trace now capture the review and its ABORT
   terminal state.
3. **P2 — source/host realization parameters are duplicated.** Source and
   host currently match (`suzuki`, order 2, steps 8, error budget `1e-6`), but
   no assertion detects future drift. This is deferred pending the P1 decision.
4. **P2 — WP branch wording is stale.** It still describes the branch as a
   Phase 0 corpus audit while the Issue is at Phase 3. Deferred pending the
   P1 decision.

## Positive evidence

- Focused LISS-0452 plus residual boundary suite: **9 passed**.
- S02 source compile check: passed with no hard diagnostics.
- `git diff --check`: passed.
- README/source preserve physicist-first equation readability and distinguish
  exact `U_t`, explicit finite `Realize`, and unsupported QPU projection.

## Disposition and terminal state

- P1 contract decision: **deferred / unresolved; requires Adjudicator decision**.
- P1 evidence-recording gap: **accepted and corrected in documentation**.
- P2 observations: **deferred** until the P1 contract boundary is decided.
- Terminal state: **ABORT**.
- No Phase 3 completion approval, completion PR readiness, implementation
  change, or merge approval is granted by this review.

## Required user decision

Choose the authoritative rejection contract for this S02 path:

- preserve the current one-qubit empty shell as an intentional diagnostic
  envelope and document it as a bounded exception; or
- require the generic fail-closed contract (`n_qubits=0` plus rejection
  provenance) and authorize a follow-up correction before completion.
